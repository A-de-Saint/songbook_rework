Utilities:
- pathwork
    -work with path strings
        -path_add, path_initialize
    -possibly system() commands

- input reader
    -read_line, (read_all)
        -usage of dynamic buffers


Functional modules:
- main

- menu 
    -display menu, return choices
    -main options:
        -new songbook
        -edit songbook
        -remove songbook
    -new songbook
        -choose name
        -choose mode (above or in-text)
        -choose export format (pdf, html, docx)
    -edit songbook
        -remove song
        -add song
        (-transpose song)

-webwork
    -get HTML into a buffer using libcurl
    -extract needed content
    -save to song_collection
        -need to make a uniform format

-[optional] some syllable magic
    -figure out where the chords go and predict them
        -problems:
            -UTF-8 to ASCII equivalents
            -cross-language
            -no clear algorithm
        -could be needed for type 2 tho


-rest to be figured out


Files:
    -global
        -program itself
        -song_collection/
            -songs/
                -for every song a .txt file
            -songlist.txt
        -songbooks/
            -songbook_list.txt (should contain info about mode and export format)
            -[some_songbook]/
                -[export-format-specific]/
                    -[tmp_files]
                -songlist.txt
                -[latest_result[format]]

Unfirom song format:
- Divide song into sections
- Best find a source where I get in-line chords (type 2)
    - Because type2 => type1 works, but type1 => type2 is risky
- Sources (priority odered):
    1. https://akordy.kytary.cz/ (type 1)
        - has chords throughout the entire song
        - easily convertible to type2
        - naming is weird
            - {url}/song/{songname}
            - for different authors it's 
                - {url}/song/{songname}-2
                - or 3, 4, so on...
    2. https://pisnicky-akordy.cz/ (type 1)
        - better naming conventions
            - {url}/{author}/{songname}
            - but jarek-nohavica instead of jaromir-nohavica, lidove, taborove, some weird shit...
        - easily obtainable chords
            - but no support for type2
            - often not throughout the entire song
    3. https://www.brnkni.cz/ (type 2)
        -impeccable naming conventions
            - {url}/{author[0]}/{author}/{songname}
        - type 2, well convertible, but unstable
            -sometimes mid-syllable



